#include <bits/stdc++.h>
using namespace std;

int main(){
    queue<string> q;

    q.push("amish");
    q.push("aryan");
    q.push("mishra");
cout<<"size before pop = "<<q.size();
   cout<<endl;
    cout<<"first element = "<<q.front();
    cout<<endl;
    cout<<"last element = "<<q.back();
cout<<endl;
    q.pop();
    cout<<"first element now = "<<q.front();
       cout<<endl;

cout<<"size after pop = "<<q.size();






}